# ABOUTME: Python dependencies for the sample Flask app.
# ABOUTME: Includes Flask and OpenTelemetry auto-instrumentation packages.
flask>=3.1.3,<4.0
gunicorn>=26.0,<27.0
# The OpenTelemetry instrumentation packages share a release series and are
# meant to move together, so they are bumped as a set rather than one at a
# time. The 0.65b0 instrumentation line pairs with the 1.44 API/SDK line.
opentelemetry-distro>=0.65b0
opentelemetry-exporter-otlp-proto-grpc>=1.44.0
opentelemetry-instrumentation-flask>=0.65b0
opentelemetry-instrumentation-requests>=0.65b0
